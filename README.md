# diabetes-prediction
This project implements a Diabetes Prediction Model using Logistic Regression. It analyzes medical features such as glucose level, BMI, and age to predict diabetes risk. The model is trained, evaluated, and validated using standard performance metrics.
